MONGODB_URI=mongodb://localhost:27017/me_api_playground
PORT=4000
# Optional: enable BASIC auth for write operations
BASIC_USER=admin
BASIC_PASS=changeme
# Optional: CORS origin to lock down in production
CORS_ORIGIN=*
# Optional: rate limit per minute
RATE_LIMIT_PER_MIN=60
